apiVersion: v1
kind: Service
metadata:
  name: blog-web-service
spec:
  type: NodePort
  selector:
    app: blog-web
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 31000
  
